# Orchestration profile — <PROJECT>

Per-project facts that `/pwnfactor:swarm` loads from `.claude/orchestration-profile.md`. Generated by `/pwnfactor:boot`. Keep it short — identifiers and rules, not prose. Fill every `<...>`; delete sections that don't apply.

## Components & parallel seams
The directories safe to build in parallel (different agents, different worktrees), and which must NOT be.
- **Components:** `<e.g. apps/server (Py), apps/web (TS), packages/shared (contracts)>`
- **Single-slot constraints:** `<e.g. all of apps/desktop shares one node_modules/build — one agent at a time>`

## Shared-contract barrier
Where cross-cutting types/contracts live; one agent lands them first and merges before consumers start.
- **Location:** `<e.g. packages/shared>`
- **Sync rule:** `<e.g. hand-mirrored pydantic<->TS pairs + barrels + parity test path>`

## Migration ritual (delete if no DB migrations)
- **Tool:** `<e.g. Alembic / Prisma / Django>`
- **Steps:** `<e.g. explicit rev id, bump head pointer, run migration test>`
- **After a migration:** `<e.g. rebuild the demo image / re-seed>`

## Mutation & rollback model (safety rails)
How irreversible/destructive actions are made safe, and how "green" is proven.
- **Rails:** `<e.g. dry-run + checkpoint-before-apply + separate rollback>`
- **Rollback semantics:** `<is rollback automatic on failure, or a separate call? what's eligible?>`
- **Proof of green:** `<the exact test that proves the rail FIRES — not that it merely exists>`

## Hotspot files (never split across parallel agents)
Files multiple units tend to touch — same-file edge = one unit.
- `<e.g. the core service file, the orders/payments engine, the shared barrels>`

## Build / refresh steps
- **Refresh after server/contract changes:** `<e.g. docker compose build + up; or "hot-reloads, none needed">`
- **Dev run:** `<command>`

## Test / lint / typecheck commands
- **Test:** `<e.g. pytest -q ; pnpm -r test>`
- **Lint:** `<e.g. ruff check . ; pnpm lint>`
- **Typecheck:** `<e.g. tsc -b ; mypy>`

## CI (what `/pwnfactor:ci` wires)
The exact per-component commands CI runs, plus gotchas.
- **Test command(s):** `<real command(s), e.g. pytest apps/server -q && pnpm -C apps/desktop exec vitest run>`
- **Lint / typecheck:** `<e.g. ruff check apps/server ; pnpm -C apps/desktop exec tsc --noEmit>`
- **Known-flaky / slow → stable subset:** `<e.g. server suite hangs ~50% → run a -k subset until issue #NN is fixed, else CI times out>`
- **Scheduled security cadence:** `<e.g. weekly pip-audit + Grype; or none>`

## Project-specific risk signals (extends the default risk tiers)
What makes a change HIGH risk in THIS project beyond the defaults (auth, secrets, migrations, fleet-/data-mutating).
- `<e.g. the payments/billing path, the webhook handler, a bulk data-export job>`

## Security sweep (pre-prod gate)
When the agent should OFFER `/pwnfactor:sweep`, and what scanners exist.
- **Prod-push trigger:** `<e.g. deploy to prod / promote the image / cut a release tag>`
- **Scanners available:** `<e.g. pip-audit, npm audit, gitleaks; or "none — built-in grep + AI review">`

## Validation — what `/pwnfactor:validate` reads (bind these at boot)
The fields that let `validate` prove a change WORKS (not just that a test passed). Without an oracle you can
only review the diff (`gg`), not prove it works.
- **`ground_truth_oracle:`** `<the cheapest external authority that answers "did the REAL thing accept this?" — e.g. a native runtime's accept-reject, a staging URL's real response (web/API), a query against the ingested store (data pipeline), the consumer call's result (library). Read ITS verdict, not your classifier's; round UP on uncertainty; where unreachable -> "verified to limit available", never fabricate certainty.>`
- **`footprint_to_validation:`** `<diff footprint -> which lanes run. e.g. narrow UI -> live drive; shared infra/contract -> FULL incl. blast-radius (verify the OTHER consumers, not just the changed thing); release/packaging -> the build/package chain + sweep. FULL != "build"; validation is DEV-FIRST.>`
- **`read_fanout_default:`** `<solo | standard | wide | saturate — read-only fan-out width (NOT a mutation dial; mutation is graph-determined by swarm). Default standard; saturate under max effort.>`
- **`cloud_local_boundary:`** `<what a cloud/CI job MAY do (read, generate, open a scoped PR) vs MUST NOT (touch secrets / prod / external infra, deploy). The oracle proof runs WHERE the oracle is reachable — CI for a cloud-reachable oracle, locally for a hardware/local-only one; secrets never enter the cloud job.>`
